<?xml version="1.0" encoding="UTF-8"?>
<project name="TestJenkins" default="ci" basedir="">
    <property name="source" value="jobs/TEST-Drupal1/workspace" />

    <target name="build"
        description="Tâche principale d'intégration continue"
        depends="clean,qa"
    />

    <target name="clean" description="Vide les répertoires d'artefacts">
        <delete dir="build/api" />
        <delete dir="build/code-browser" />
        <delete dir="build/coverage" />
        <delete dir="build/logs" />
        <delete dir="build/pdepend" />

        <mkdir dir="build/api" />
        <mkdir dir="build/code-browser" />
        <mkdir dir="build/coverage" />
        <mkdir dir="build/logs" />
        <mkdir dir="build/pdepend" />
    </target>

    <!-- PHP Depend -->
    <target name="pdepend">
        <phpdepend file="${source}/www/sites/all/themes/dm_skin">
            <logger type="jdepend-xml" outfile="build/logs/jdepend.xml" />
            <logger type="jdepend-chart" outfile="build/pdepend/dependencies.svg" />
            <logger type="overview-pyramid" outfile="build/pdepend/overview-pyramid.svg" />
        </phpdepend>
    </target>

    <!-- PHP Mess Detector -->
    <target name="phpmd">
        <phpmd file="${source}/www/sites/all/themes/dm_skin">
            <formatter type="xml" outfile="build/phpmd.xml" />
        </phpmd>
    </target>

    <!-- Détection de portions de code copié-collé -->
    <target name="phpcpd">
        <phpcpd file="${source}/www/sites/all/themes/dm_skin">
            <formatter type="pmd" outfile="build/logs/pmd-cpd.xml"/>
        </phpcpd>
    </target>

    <!-- Stats (nombres méthodes, nombres de lignes de code etc... -->
    <target name="phploc">
        <exec logoutput="true" dir="${project.basedir}" command="phploc --log-csv 'build/logs/phploc.csv' '${source}/www/sites/all/themes/dm_skin'" escape="false" />
    </target>

    <!-- Phpdoc -->
    <target name="phpdoc">
        <phpdoc title="API Documentation"
                destdir="build/api"
                sourcecode="false"
                output="HTML:Smarty:PHP">
            <fileset dir="${source}/www/sites/all/themes/dm_skin">
                <include name="**/*.php"/>
            </fileset>
            <projdocfileset dir=".">
                <include name="README"/>
                <include name="INSTALL"/>
                <include name="CHANGELOG"/>
            </projdocfileset>
        </phpdoc>
    </target>

    <!-- Phpcb -->
    <target name="phpcb">
        <exec logoutput="true" command="phpcb --log 'build/logs' --source '${source}/www/sites/all/themes/dm_skin' --output 'build/code-browser'" escape="false" />
    </target>

    <target name="qa" description="Lance les outils d'analyse">
        <phingcall target="pdepend" />
        <phingcall target="phpmd" />
        <phingcall target="phpcpd" />
        <phingcall target="phploc" />
        <phingcall target="phpdoc" />
        <phingcall target="phpcb" />
    </target>

</project>